# GO-Fiber
Explore Implement Framework Fiber

# CONFIG DOCKER

## Build image Docker

Dari folder project jalankan:

``docker build -t go-fiber-app`` 

## Jalankan container

``docker run -d -p 3000:3000 --name fiber-container go-fiber-app``

=============================================

Jalankan ini untuk config docker compose

docker compose up -d
